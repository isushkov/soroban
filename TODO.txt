stage:
  stage-1 -> 1/10 + add title Stages
первый стейдж гет-реади повторно
not-study: goal колонка не удалена полностью
добавить подчеркивание нижнее

tests прогнать до конца:
  yes-no
  input
  stydy/run
  training/exam
    exam: -stage_start -timing

tests-script:
  create:
    check 3seq-random
    note if cover range 1-3
перенос строки если operations не умещается в указанный width
todo:
  oop:
    params to object
    sequence to object Exercise
  вынести todo из кода в main.py
  redo params + redo progressions:
    "<first_sequence>,<sequence>,<sequence>,..."
    decimal.precision -> range.procision
  upd readme

backlog:
  дополнительные возможности:
    прочитать книги:
      decimal
      отрицательные
      умножение/деление
    реализовать:
      decimal
      отрицательные
      умножение/деление
  mental-mode:
    в процессе работы отображать stage по одному числу - числа должны маргать
    как готово вернуть всю строку
  extra возможности:
    прочитать книги:
      квадратные корни, другое?
  добавить локаль для других языков через переводчик
