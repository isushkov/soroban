tests:
  stage:
    yes-no
      passed
      repeat
    input
      passed
      repeat
    min num_per_stage
      yes-no
        passed
        repeat
      input
        passed
        repeat
    max num_per_stage
      yes-no
        passed
        repeat
      input
        passed
        repeat
  run:
    yes-no
      passed
      repeat
      change num_per_stage
        passed
        repeat
    input
      passed
      repeat
      change num_per_stage
        passed
        repeat
study: program to csv + comment

tests-script:
  create:
    check 3seq-random
    note if cover range 1-3

todo:
  oop:
    params to object
    sequence to object Exercise
  вынести todo из кода в main.py
  redo params + redo progressions:
    "<first_sequence>,<sequence>,<sequence>,..."
    decimal.precision -> range.procision
  upd readme

backlog:
  дополнительные возможности:
    прочитать книги:
      decimal
      отрицательные
      умножение/деление
    реализовать:
      decimal
      отрицательные
      умножение/деление
  mental-mode:
    в процессе работы отображать stage по одному числу - числа должны маргать
    как готово вернуть всю строку
  extra возможности:
    прочитать книги:
      квадратные корни, другое?
  добавить локаль для других языков через переводчик
