stage_start:
  дорисовывать пустой row
  дорисовывать table_b
  возвращать каретку
перенос строки:
  training/exam

repeat - объекты не создаются новые экземпляры

tests run/study:
  cases:
    ++++
    ++E++
    +EE+++
    E++++
  study/training/yes-no
  study/training/input
  study/exam/input
  run/training/yes-no
  run/training/input
  run/exam/input
tests create/analyze:
  run scripts
  check 3seq-random
  note if cover range 1-3
todo:
  redo params + redo progressions:
    "<first_sequence>,<sequence>,<sequence>,..."
    decimal.precision -> range.procision
  upd readme

backlog:
  дополнительные возможности:
    прочитать книги:
      decimal
      отрицательные
      умножение/деление
    реализовать:
      decimal
      отрицательные
      умножение/деление
  mental-mode:
    в процессе работы отображать stage по одному числу - числа должны маргать
    как готово вернуть всю строку
  extra возможности:
    прочитать книги:
      квадратные корни, другое?
  добавить локаль для других языков через переводчик
