tests run:
  1 stage (1 row)
  1 stage (3 row, last uniq-len)
  3 stage (1 row)
  3 stage (3 row, last same-len)
  3 stage (3 row, last uniq-len)
  %% run-new training one-row   yes-no no_errors
  %% run-new training one-row   yes-no err_1st_stage
  %% run-new training one-row   yes-no err_3th_stage
  %% run-new training one-row   yes-no err_3th_stage_x3
  %% run-new training one-row   input  no_errors
  %% run-new training one-row   input  err_1st_stage
  %% run-new training one-row   input  err_3th_stage
  %% run-new training one-row   input  err_3th_stage_x3
  %% run-new training multi-row yes-no no_errors
  %% run-new training multi-row yes-no err_1st_stage
  %% run-new training multi-row yes-no err_2th_stage
  %% run-new training multi-row yes-no err_2th_stage_x3
  %% run-new training multi-row input  no_errors
  %% run-new training multi-row input  err_1st_stage
  %% run-new training multi-row input  err_2th_stage
  %% run-new training multi-row input  err_2th_stage_x3
  run-new exam     one-row   input  no_errors
    bug extra footer after input
  run-new exam     one-row   input  errors
  run-new exam     multi-row input  no_errors
    bug extra footer after input
  run-new exam     multi-row input  errors

  study   training one-row   yes-no no_errors
  study   training one-row   yes-no err_1st_stage
  study   training one-row   yes-no err_3th_stage
  study   training one-row   yes-no err_3th_stage_x3
  study   training one-row   input  no_errors
  study   training one-row   input  err_1st_stage
  study   training one-row   input  err_3th_stage
  study   training one-row   input  err_3th_stage_x3
  study   training multi-row yes-no no_errors
  study   training multi-row yes-no err_1st_stage
  study   training multi-row yes-no err_2th_stage
  study   training multi-row yes-no err_2th_stage_x3
  study   training multi-row input  no_errors
  study   training multi-row input  err_1st_stage
  study   training multi-row input  err_2th_stage
  study   training multi-row input  err_2th_stage_x3
  study   exam     one-row   input  no_errors
  study   exam     one-row   input  errors
  study   exam     multi-row input  no_errors
  study   exam     multi-row input  errors

tests create/analyze:
  run scripts
  check 3seq-random
  note if cover range 1-3
todo:
  redo params + redo progressions:
    "<first_sequence>,<sequence>,<sequence>,..."
    decimal.precision -> range.procision
  upd readme

backlog:
  дополнительные возможности:
    прочитать книги:
      decimal
      отрицательные
      умножение/деление
    реализовать:
      decimal
      отрицательные
      умножение/деление
  mental-mode:
    в процессе работы отображать stage по одному числу - числа должны маргать
    как готово вернуть всю строку
  extra возможности:
    прочитать книги:
      квадратные корни, другое?
  добавить локаль для других языков через переводчик
